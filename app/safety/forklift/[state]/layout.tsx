// Metadata for state pages lives in [state]/page.tsx (generateMetadata),
// which sets title, description, canonical, robots, OG, and Twitter tags.
export default function StateLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
