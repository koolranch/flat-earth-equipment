## Learned User Preferences
- Around checkout, Stripe webhooks, training, certificates, auth, deep links, and enterprise/trainer dashboard flows, inspect the existing route/schema/handler first and preserve current customer behavior unless the user explicitly approves a change; prefer additive, guarded changes with focused QA before commit/push/deploy because the user is risk-averse about breaking live revenue (training and parts), and for larger mobile/training/auth rollouts split work into numbered phases (1a, 1b, …) each with an explicit "do not change" list (Stripe webhooks, quiz-complete insert shape, exam submit, certificate issue) and a PASS/FAIL regression table before promoting to production; for production authorization fixes on enterprise/org/trainer routes, never trust caller-supplied `org_id` without a server-side `org_members` check, use neutral commit messages (no exploit how-to detail), and coordinate deploy timing with the user; when asked to deploy, report the commit or deploy status and the live production URL once verified.
- For customer-facing email, credential, or quote drafts, provide plain Markdown that copies cleanly into Gmail without code-block styling or dark-mode background bleed; sign forklift training / certification customer emails as Sam (never Chris); transactional credential/welcome emails should promote both the iOS App Store and Google Play as a secondary app block while keeping proven primary web CTAs unchanged unless the target path is verified as a claimed Universal/App Link; app nurture/reactivation for Forklift Certified should brand as the app (not Flat Earth Equipment corporate) and may send from the verified `training@getforkliftcertified.com` domain; Track A (100% unpaid) is the purchase nudge (weekday AM after ≥36h, web checkout in the email) and Track B (20–99% unpaid) is finish-training only with no checkout—weekday AM after a ≥48h stall, not Monday-only; do not pitch checkout to mid-progress users; the reactivation cron measures the stall from enrollment `updated_at`, so any later enrollment touch resets the clock and the cron silently skips the user (a recurring miss)—when the true first-100% timestamp is past the floor, send the A1 manually and log it so the cron won't double-send, and QA/demo addresses (e.g. `@getforkliftcertified.com` test accounts) must be excluded from the reactivation engine; when a welcome/credential email bounces, compare the Stripe checkout email to the customer name for plausible typos (transposed letters, wrong domain like `gamil.com`), confirm the corrected address with the customer, then update auth + `profiles` email, reset password, and resend credentials (plain Resend resends may omit change-password nags when requested); when checking whether recent buyers got access, verify welcome/access delivery and enrollment/quiz activity first—missing certificate emails usually means the final exam is not passed yet, not a failed checkout; paid app users stuck on a paywall after a live `orders` row are a local refresh issue (Restore purchase while signed in)—do not reset an active password or issue a certificate before a passing exam attempt; when asked for purchase/signup/app-sales counts, query Supabase `orders` and `auth.users` using `stripe_session_id` channel patterns rather than reporting transient one-off figures.
- For product listings and parts marketing sourced from vendor materials, rewrite copy in original wording, avoid naming vendor brands such as TVH unless explicitly requested, do not claim specific source/availability percentages, use clean watermark-free product images rather than publishing vendor-watermarked photos, and focus on the vendor's fast-moving stocked SKUs rather than chasing hard-to-find parts; for Seats/Seat cushions/Seat covers use the same `/parts-watch` image tray as other Mag parts (AI-clean + Approve; raw vendor photos never go live)—skip-comps still hold Bobcat `7505149` / `6669135`; do not hunt more construction seats for Shopping; for lithium kit SKUs use a real per-SKU Lithium Rhino battery photo on the PDP, Stripe, and Merchant (never the shared brand PNG or a vendor All-in-1 kit collage), and list included conversion items in kit PDP copy only (not `-S` battery-only SKUs) without FSIP/vendor kit-pack branding; never run bulk TVH portal cost lookups (high inquiry volume flags the account)—the user can search the vendor portal for PNs not yet listed and provides wholesale costs ad hoc only when already known from a normal order (skip when Magnasource is at or under cost); brand fault-code hubs, high-traffic `/insights/*` pages, and educational how-to pages under `/parts/construction-equipment-parts/` (e.g. Bobcat serial-number finder) should diagnose/educate first then soft-sell related live catalog with working CTAs (e.g. Genie scissor-lift codes → `/parts/aerial-equipment/genie-scissor-lift-error-codes` parts hub with stocked SKU links, chargers → `/battery-chargers` + lithium, forks → attachments/forks SKUs, JCB codes → likely parts categories + browse-all, serial how-tos → brand `/<brand>-serial-number-lookup` plus stocked tracks/glass—not rentals-first) rather than lead-form-only or dead links, consolidate cannibalizing insight URLs into the canonical parts hub when one exists, rewrite AI-slop copy and replace AI-generated images (keep canonical/title/H1/OG/URL untouched) plus remove irrelevant internal links, and when an insights page covers forklift certification promote app and online training only (never in-person training); when offering economy and standard versions of the same part, list both as separate SKUs with mutual cross-links rather than a single listing; when a part is aftermarket, say so clearly once and use confident fitment copy—do not keep restating that it is not OEM, and avoid scare-off hedges that imply uncertain OEM revisions will break fit; do not use generic “confirm before ordering” / “confirm cab or machine before ordering” language on PN-first Buy Now listings (glass, JCB, seats, aerial, etc.)—keep real fitment notes only (track width/serial prefix, backorder availability, voltage/controller), and on heavy/high-ticket aftermarket PDPs include identity specs that prevent wrong-part returns (port count, thread, no electric ring, weight); highlight the 2-year aftermarket warranty on eligible JCB, Bobcat, forklift parts, and rubber tracks (exclude chargers, controllers, charger modules, and any core-charge SKU); unpriced or `$0` parts must show request-quote UX, never a `$0` price; when changing the Google Merchant feed, keep already-approved SKUs (including JCB brand-logo heroes) and never rewrite `g:id` on those items; if Merchant caches stale track photos, cache-bust `image_link` with a `?v=` query and keep the same per-slug path/`g:id`.
- For parts, serial-lookup, and geo/city SEO work, preserve user-provided OEM/part-number intent and honest service scope (no LocalBusiness or on-site claims where the business only ships parts); sales are US-only—prefer Vercel edge/firewall geo-blocks for abusive non-US bot traffic (Singapore first) over changing checkout or catalog flows; never add fabricated `aggregateRating` in JSON-LD; do not publish public `llms.txt` or `disavow.txt` (disavow stays under `scripts/seo/` for Search Console upload only); on high-traffic organic pages explicitly enumerate per-change SEO risk (canonical/title/OG/URL untouched, structured data only additive, no redirects) before deploying; build Spanish certification SEO on `/es/*` on the main domain (inheriting existing authority and hreflang)—parked alternate domains such as `forkliftcertification.com` should 301 to `/es/safety` rather than splitting crawl/link effort onto a separate site; for forklift certification organic SEO, do not rewrite `/safety` ad hero/CTA wiring for rankings—use paid for head terms; improve existing `/insights/*` clusters and `/safety/forklift/{code}` state/city long-tail (Labs-signal states NJ/WI/TN/CA first) with additive title/H2/FAQ and internal links to `/safety`, and track via DataForSEO snapshots under `scripts/seo/rank-snapshots/forklift-cert-*`; for managed category revenue programs (charger modules, rubber tracks, lithium batteries, cab glass), run measure→convert-already-ranking-URLs→expand phases aimed at purchases and high-intent queries (not vanity traffic), treating Bing/Yahoo as a second organic index that ranks exact OEM PNs and serial-lookup tools rather than category hubs (do not add Bing Ads budget or weekly DataForSEO Bing rank jobs), keep weekly DataForSEO STATUS under `docs/projects/*-revenue/` (including `cab-glass-revenue`), reuse existing Cloud Agent `DATAFORSEO_LOGIN`/`DATAFORSEO_PASSWORD` secrets without re-prompting, and never let those automations touch checkout or Stripe webhooks.
- When changing web certificate, enrollment, training, deep-link, or org/trainer flows, verify the change does not break the companion mobile app; when refreshing `/safety` or state-page app marketing imagery, prefer real iPhone App Store screenshots via the seeded demo account `demo@flatearthequipment.com` (login details in the mobile repo `assets/screenshots/CAPTURE_GUIDE.md`, never store passwords here) over stale dev-simulator captures, use locale-specific assets under `public/media/safety/app/{en,es}/`, and present app shots in the phone-frame `ScreenshotCard` variant (not browser chrome) so mobile-first CTAs read as the app product.
- For multi-seat or org purchases where some employees lack personal email, create unique placeholder learner accounts and hand credentials to the org owner manually instead of auto-emailing invites; provide a clean manager summary plus a separate credential sheet; hold roster rows with duplicate emails until each learner has a unique address; on Google Workspace/Microsoft 365 try `email+alias@domain` before placeholders when one inbox serves multiple learners; when seat-invite claim links fail, bypass by creating accounts directly, attaching to the org, enrolling, and handing the supervisor a credential sheet (skip auto-invite resends unless requested); for custom enterprise multi-seat invoices use Stripe Checkout Sessions with `automatic_tax: { enabled: true }` (not flat Payment Links that omit tax) and share the full checkout URL including the `#` fragment.
- When the user provides a batch parts pricing/availability list (typically 10 at a time), apply prices in order; entries marked `NA` mean remove the listing from the website, and `*backordered` or any limited-stock annotation should surface a "contact us to confirm availability before ordering" note on the product page rather than disabling purchase; for industrial seat catalog rollouts prioritize hub-primary SKUs and brand order Caterpillar → JLG → Genie → JCB → Skytrack → Bobcat over alphabetical Advance stubs, benchmark sell price via Magnasource or scrapeable comp sources (not bulk TVH cost lookups)—construction seats as a paid category are a dead end (aftermarket floors sit on cost), so do not hunt more for Shopping; seats/cushions/covers still use the same `/parts-watch` Apply path as the rest of the TVH catalog (do not blanket-skip seats from Mag reprice—skip-comps plus Mag-under-cost hold and the 1.5×/3× verify band already protect thin-margin rows); live JCB `40/910649` replacement seat stays organic-only at $1950 until a trusted cost exists (cleaned hero at `public/images/parts/seats/jcb-40-910649-replacement-seat.jpg`, Merchant `g:id` `40910649` unchanged); review the past week's abandoned/unpaid Stripe parts checkouts weekly for price checks and image confirmation—reprice ~5% under the user-provided Magnasource comp, swap brand-logo heroes for cleaned real photos, and keep the Merchant Item ID (GA4/Vercel `begin_checkout` fires only on training CTAs, so parts abandonment is visible only via Stripe unpaid sessions). For TVH-sourced OEM rollouts (MPA/CPA/IPA/glass), prefer Intella and Magnasource as trusted same-network comps—if they show the PN priced and in stock, mark ours stocked and apply Buy Now (~5% under lowest trusted comp) and leave out-of-stock/special-order/price-hidden rows as `quote_only`; Magnasource is the ongoing inventory + sticker source of truth for all TVH-network equipment parts (JCB, Genie, JLG, Skytrack/Skyjack, Bobcat, and other OE-prefix brands already in catalog, plus cab glass and equipment electrics such as sensors/switches/joysticks—not a hand-picked SKU list), excluding rubber tracks, lithium, Navitas, FSIP chargers/Sevcon, reman charger modules, and charger category shelves (`lib/pricing/magWatchUniverse.ts`); scrape public Magnasource itemdetail only through Firecrawl proxies (Magnasource cannot attribute those reads to Flat Earth IP/Vercel/dealer login; bulk TVH portal cost lookups are what flag the account); Mag on-hand is the shared TVH warehouse quantity Magnasource drop-ships from (the same pool we sell from, not Magnasource-owned stock and not `parts.is_in_stock`); Mag 0 / backorder / special-order / non-cancellable on Buy Now, after two consecutive reads, is a sold-out pull (`quote_only` + `is_in_stock: false` + archive the Stripe price, because `/api/checkout` trusts a client `priceId`); Mag limited/1–2 is a review flag only and never auto-disables—when Magnasource shows limited/1 on hand but the user states actual vendor stock, trust the user's count for Buy Now; Mag public price is the sticker to undercut (~5%), not dealer cost — never write Mag sticker into `cost_wholesale`; unpriced quote-only + Mag in stock + Mag sticker → generate first sell from Mag (~5% under) and leave true cost for the first PO; already-priced Buy Now sitting above Mag is a reprice review, not an automatic 5% cut — confirm TVH cost first, especially if Mag looks impossibly cheap vs other comps or vs known cost; Buy Now sitting far below Mag from eBay/Amazon marketplace cards is the same verify band (junk floors are not trusted comps)—when cost confirms the same item, realign ~5% under Mag; do not auto-reprice, auto-relist, or auto-publish quote-only stubs (relist needs user stock confirm); `/parts-watch` Reprice applies Mag cuts/raises with hold (Mag at/under cost or operator lock) and verify (no cost + 1.5×/3× gap, usually pack-vs-each)—do not bulk-apply Mag raises on small Skyjack/Genie/JLG electricals (historically no clicks); quote-only stubs Mag shows in stock surface as a Publish panel hard-gated on an approved real product photo (logos/empty `image_url` do not count; vendor og:image never goes live raw; Publish accepts one row or checkbox batches up to 15, each SKU still server-gated; Limited/1 skips new listings); do not auto-set `parts.image_url` from a raw Mag scrape (hero facts may be recorded on `mag_watch.hero`; seats/cushions/covers use the same tray)—the `/parts-watch` image tray is the review path (include cheaper/low-ticket photo-queue rows, not high-ticket only), with AI watermark-clean/enhance available on every SKU (held/failed retries start from the original Mag raw, never the bad AI file; park Mag photos that are the wrong part) and user approve of the cleaned hero before it goes live; weekday Cloud Automation runs `scripts/pricing/mag-watch.ts` read-only on the confirmed A/B/C/D cadence (Buy Now ≥$300 every weekday, $100–299 Wed+Fri, <$100 Monday, quote-only stubs Tue/Thu/Fri at 200/run) plus a weekday sell-out overlay of live Buy Now whose last Mag qty is ≤3 (capped at 80, fresh scrape)—leave C weekly and do not scrape the whole catalog harder; the same weekday job downloads up to 40 Mag in_stock identity-ok photo-gap heroes into the private image tray (32 quote-only / 8 Buy Now; skip limited/LTL/skip-comps/already-real; never sets `image_url`) so `/parts-watch` can AI-clean → Approve → Publish without a separate intake command (cost still at first PO; Limited/backorder/LTL/≥75 lb/skip-comps stay quote-only); the noindexed `/parts-watch` dashboard can Pull/Relist/Reprice/Publish using the same gates as `scripts/pricing/mag-watch-apply.ts` (top strip is vendor-warehouse units × our sell: Sellable now / Ready to list / Waiting on a photo) (audit in `parts_ops_audit`; Merchant XML still needs rebuild+commit after Buy Now flips); when the user supplies a comp price plus a product image for a PN, that signals the item is in stock and good to sell (Buy Now) even with no `cost_wholesale`—the user pulls actual cost at first purchase order; in per-PN batch sessions the user may instead give actual wholesale cost plus comps and a vendor-portal screenshot "to rework" into a clean watermark-free hero, and when the user flags a PN's comps as too cheap or the PN as out of stock, skip that listing entirely rather than publishing a thin-margin row (stored skips + costs in `data/seats/skip-comps.json`: Bobcat `7505149` operator seat cost $403.36 vs Magnasource $399 as of 2026-08-26 — cleaned hero at `public/images/parts/seats/bobcat-7505149-seat-assembly.jpg`, do not list until Mag moves; Bobcat `7338638` hydraulic cylinder cost $470.48 vs All States / All Skid Steers $476.67 — skip; Bobcat `6669135` operator seat cost $129.43 vs aftermarket floor ~$148–$209 — skip, superseded by `7505149`)— so comp-priced listings are sellable immediately but paid-ad bid ceilings should wait for confirmed cost (or use a conservative same-batch observed-margin proxy, e.g. ~13–14% on big-ticket free-freight TVH items); cab glass is TVH drop-ship but Mag-style (sticker ~5% under Magnasource, $25 TVH-band freight at checkout—not `free_freight` as a newer-seller default, and not the free-ship tracks/high-margin Shopping lane; organic PN pack only—do not add glass to Shopping) so weekday glass profit is kept rather than conversion-at-all-cost, and Merchant XML must show $25 Ground not $0; for FSIP-sourced golf-cart electrical (Navitas controller kits, Delta-Q/QuiQ chargers, Sevcon DC/DC converters), use logged-in FSIP dealer cost (not the public list) for margin, use Cloud Electric as the primary public same-vendor-network comps—price at a clean number slightly under those comps when dealer economics allow; if the usual margin floor would price above that primary free-ship Cloud Electric sticker, undercut the Cloud Electric price instead and accept thinner margin rather than overshooting; when a comp looks impossibly low vs current dealer cost treat it as stale and keep a profitable clean price rather than forcing an unprofitable undercut; match free freight when comps offer it (cover outbound freight from margin via `free_freight`); prefer brand-new Buy Now SKUs over reman/core-exchange or repair-and-return-only rows; verify cart/controller fitment carefully before publish (wrong kit recommendations create returns); never publish vendor/TVH-prefixed or house SKUs (strip via `lib/parts/tvhOePrefixes.ts`; no `TSA/` or SY-style house numbers on the live site). For rubber track listings: SEO targets machine model + "tracks" first (slug like `bobcat-t650-rubber-track-450x86x52`), OEM PN secondary where extractable, size string long-tail only, track URL slugs use `{brand}-{model}-rubber-track-{size}` without tread suffixes like `-block` when the size already identifies the SKU, and vendor `TSA/` part numbers never appear publicly (internal ordering reference only); sell per track with the quantity selector defaulting to 2 plus a "most operators replace both sides" note (never force pairs); track PDPs use a primary Buy Now straight into Stripe (same `{priceId, slug, qty}` checkout path) with Add to Cart secondary; verify every vendor serial-prefix break before fitment claims—standard sizes differ by model (e.g. T770 450x86x55 with 55 links, not T650's 450x86x52; T190 320x86x49 with 49 links, not T650's 320x86x52); when breaks return different SKU sets on the same model, only the SKU universal across all breaks gets blanket "fits all" copy and the rest get prefix-scoped listings (T190 wide/alt-pitch pattern); skip vendor out-of-stock SKUs; publish from intake via `scripts/add-{brand}-{model}-rubber-tracks.ts`; because tracks ship free with a 2-year warranty, price at or near comp list (protecting margin) rather than the strict 5%-below rule—free freight is table stakes vs Grizzly/Fortis/SkidHeaven; do not lead Shopping with T650 450x86x52 (weak sticker vs those comps); live Shopping Item IDs are the 400-wide C-pattern T190/T550/T590 (`RT-T190-400X86X49-C` and matching T550/T590 IDs; vendor `TSA/SY400X86X49C`; keep sale $949—do not match cheaper 320mm cards) plus JD 331G 450x86x58 C; strikethrough list pricing uses `parts.metadata.list_price_cents` (PDP line-through + Merchant `g:price` list / `g:sale_price` charged so the card and landing page match)—live on those 400-wide C SKUs at list $1149 / sale $949; leave narrow 320s (`RT-T190-320X86X49-C`, T650 `RT-320X86X52-C`) out of Shopping (vendor cost ~$800 vs comps ~$861–935); do not add T650 450; hold the planned Cat 259D / T770 Shopping expansion—at break-even bids the tracks auction is owned by cheaper free-ship comps (rank-lost impression share ~80%), so tracks Shopping is a cheap PN-presence play rather than a volume lane, and new spend priorities rank ahead of new Takeuchi/Kubota seat or glass SKUs; when adding Bobcat OEM cross-refs, put the live OEM (`7316759`) in T190/T550/T590 C-pattern PDP H1/title and Merchant title only (not zigzag, not as Merchant `g:id`/`mpn`) and treat vendor `SY*` codes as size/style not OEM numbers; vendor inbound on `SY400X86X49C` is not a reason to raise Shopping CPC or cut the $949 sale; CNY-presentment / non-US checkout starts are not Shopping signals; do not use Merchant Center Price competitiveness on “All products” (crawled mixed catalog of seats/forks/etc.) to cut track prices—that view is not the XML keep-set; the `/parts` rubber-tracks category CTA should link the `/rubber-tracks` hub (not only a catalog filter).
- Before launching paid Google/App campaigns to `/safety` or `/es/safety`, prioritize "Critical Before Ads" fixes: ≥44×44px tap targets on primary/sticky CTAs; web "Buy now" CTAs must call `createTrainingCheckoutSessionFromForm` (not `#pricing` scroll); app-store badges as `<button type="button">` with `gclid`/UTM passthrough via `lib/analytics/app-download.ts`; mobile hero and sticky CTA are web-checkout-first for both ad and organic traffic (server-side `detectSafetyTrafficSource` still tags `gclid`/Google Ads UTMs/`_gcl_aw` vs organic for analytics/placement names; app badges stay secondary); desktop always web-first; Spanish ads land on `/es/safety` (legacy `/certificacion-montacargas-espanol` redirects with params)—do not send Spanish paid traffic to app-only landings on `certificacionmontacargas.com` until EN blended CPA is proven. Remaining cert Search is Ohio + Florida (Arizona paused as highest CPC); do not unpause AZ/TX/PA/GA/IN/NC/TN or scale $49 cert Search. State LP URLs on `/safety?state={name}` and `/safety/forklift/{code}` must stay functional when editing safety LPs. Safety LPs and the site footer must show dual App Store + Google Play badges via `MobileAppBadges` (`IOS_APP_LIVE=true` in `lib/app-store/links.ts`; `GooglePlayBadge` is a thin alias)—never Play-only. Never gate `app/api/checkout` on analytics flags such as `FEATURE_GA`. Favor web Stripe checkout on ad traffic for full margin (app store purchases lose ~15–30%); if testing Forklift Certified marketing domains (`getforkliftcertified.com`, `certificacionmontacargas.com`) as ad LPs, the primary CTA must deep-link to FEE `/safety` or `/es/safety` checkout—never a second Stripe/webhook stack on those hubs. Persist `gclid`/`gbraid`/`wbraid` (and funnel state) client-side via `CaptureClickIds` / `fee_*` cookies / localStorage, into Stripe Checkout Session metadata via `lib/attribution/getClickIds.ts` and `ClickIdsHiddenInput`, and from the Stripe webhook onto `orders.gclid`/`gbraid`/`wbraid`/`funnel_state` for true ROAS—not just URL/cookie passthrough. For 30-day profitability checkpoints, compare Google Ads spend to Supabase app+web `orders` revenue (blended CPA)—not Ads-dashboard attributed ROAS (iOS IAP is invisible to Ads; dashboard conversions are mostly `app_download_click`, and `orders.gclid` has been empty); if any cert spend remains, bid on GA4 purchase, and do not scale $49 cert Search. Training cannot be the $5k–$10k/month profit engine—that is parts Shopping, with tracks and high-margin TVH drop-ship PNs preferred over lithium/Navitas/reman (vendor-stocked, no core/HazMat, less admin); GFC Employer Search (`24158336820`) is killed/paused (user proceed)—do not treat Employer as live or as a project to keep when reviewing ads, and do not unpause without an explicit proceed; GFS Cleveland Search (`24250421199`) is a separate non-GFC project and stays live; GFC Operator Search (`24184008234`) is paused (2026-09-15, script `scripts/ads/2026-09-15-gfc-operator-pause-campaign.py`) after ~$233 weekday spend and 1 counted gclid $49 paid post–Sep 5 negatives (1/8 cart close; head terms were clean)—GFC/FEE `/safety` PDPs and Stripe stay live; do not unpause Operator without an explicit proceed; leftover how-to negatives stay, do not block head terms like forklift certification/training/license, lock GFC Search geos to US Presence-only because Presence-or-interest leaks Canada, and port Employer city/how-to/foreign negatives via `scripts/ads/2026-09-05-gfc-operator-geo-howto-negatives.py`; do not rewrite GFC Stripe chrome, success/cancel URLs, `display_name`, or the Flat Earth billing explanation from a single inferred GFC→FEE checkout hop—that copy exists so the card statement is not a surprise. Next paid channel is Google Shopping for rubber tracks after the Merchant XML feed is Active (not the Found by Google crawl); use a standard Shopping campaign on Merchant Item IDs—do not use Performance Max in this account (Ads `3466711027` has no Display-off toggle; PMax campaign `24147212306` burned the $30/day budget on CONTENT/Display and is paused); PMax uses no keywords—listings match from the Merchant title/image/Item ID; Shopping final URLs must be `https://www.flatearthequipment.com/rubber-tracks` or the T190 PDPs—never Shopify-style `/collections/` or `/products/` paths or unrelated Kubota/excavator sizes; track test is $30/day with listing groups by Item ID (not Products=All)—Merchant Center Edit campaign cannot pick a single SKU; US-only location; bid only `flatearthequipment.com (web) purchase` (never `app_download_click`); live includes on standard Shopping campaign `24146723448` (“Rubber Tracks Shopping T190”) are T190/T550/T590 400-wide C (T550/T590 added to the T190 ad group at $2.65) and JD 331G 450x86x58 C at a $1.55 tax-haircut bid until the user confirms MTC multistate + Texas resale certificates are on file with the tracks vendor (then lift 331G toward the clean break-even); narrow 320s stay out; Enabled + Eligible Item IDs + Approved ads can still mean `primary_status` PENDING / MOST_ADS_UNDER_REVIEW and 0 Shopping impressions—do not treat site traffic as paid Shopping until Network = Search and impressions appear; leftover clicks on paused PMax `24147212306` are Content network, not Shopping; the Google Ads MCP tools are read-only—apply bid/campaign mutations via one-off scripts under `scripts/ads/` that reuse the local MCP server's credentials at `~/.mcp-servers/mcp-google-ads` (the live token file is the env-configured `credentials.json`, not the stale `token.json`), and when a structurally correct Shopping campaign sits stuck in review a small bid edit forces Google to re-evaluate. Shopping wave 1 is live as standard campaigns with manual CPC, listing groups by Item ID, and profit-derived bids: Lithium Golf Kits `24175252534` (paused 2026-09-02 after zero Stripe checkout starts; PDPs/Merchant stay live) and Navitas Kits `24170389196` (paused 2026-09-05 after 123 clicks / $146 and zero Stripe checkout starts; PDPs/Merchant stay live — script `scripts/ads/2026-09-05-navitas-pause-campaign.py`) alongside the tracks campaign `24146723448` (bids cut to profit ceilings, John Deere G-series ad group added from abandoned-cart evidence); expansion is data-gated—kill (pause) an Item ID after one max-CPA (50% of unit profit) of spend with no sale, scale budget only when CPA ≤ 35% of unit profit; do not raise CPC when lost impression share is budget (raise daily budget instead)—modest bid lifts are OK when lost-IS is rank, still under the profit-ceiling max-CPA—except after the first lithium Shopping week do not raise lithium CPC when there are clicks and 0 purchases even if lost-IS is rank (that buys more no-sale traffic; kill remains per Item ID at one max-CPA); judge Shopping/parts attribution by Stripe checkout metadata (`gclid`/`gbraid`/UTM) and checkout starts (completed + expired sessions matched to live listing-group Item IDs), not Ads-dashboard conversions or Merchant XML membership—a PN in the feed is not paid Shopping when that `g:id` has zero clicks or is not in the live listing groups—heavy clicks with zero checkout starts is a stronger cut signal than zero sales and justifies trimming weak Item IDs ahead of their kill lines (approved Sep 1: lithium 48V 65Ah and 36V 105Ah excluded via `scripts/ads/2026-09-01-lithium-pause-65ah-36v.py`; campaign-level pause of `24175252534` on Sep 2 superseded running remaining keepers to kill lines — script `scripts/ads/2026-09-02-lithium-pause-campaign.py`); excluding a Shopping Item ID means removing the positive listing-group unit and recreating it as a negative unit under the same subdivision in one atomic mutate—the API silently accepts `status=PAUSED` on listing-group UNIT criteria and ignores it; High-Margin traction comes from unique high-ticket OEM PNs (live JCB LH joystick `332/X6237`; live Bobcat `7123864` excavator swivel at $2799 free freight, cost $2411.02; JCB `716/C8932` throttle pedal cost $188 / sell $359 + $35 freight is in Merchant XML but not in live High-Margin listing groups; next quote-only stub `332/X6239` needs Mag + cost before publish) with a real photo, a winnable sticker, and a known/proxy cost—not from seats, glass, filters, or raising CPC on quiet Tennant/Skytrack rows; do not cut the live joystick price; do not undercut OEM on unique high-ticket PNs just to look cheaper (Bobcat shop Merchant lists `7123864` at $2823.49 with free shipping over $99—a small sticker gap will not move OEM buyers); do not treat Merchant Center product clicks as Ads purchases; drop-ship categories (tracks, lithium, Navitas) launch before reman charger modules (wave 2) because reman core-deposit handling costs ~$30/order of admin and the $350 core deposit drags conversion on cold traffic; the account-level primary conversion set includes `app_download_click`, so every new campaign must pin campaign-level goals to the web purchase conversion only. Wave 2 (charger modules) plus High-Margin Parts (fifth Shopping campaign) are now live and serving; in the early weeks review search terms per campaign to prevent wasted spend—Shopping has no positive keywords (Merchant titles are the targeting), so query control is negatives only: exact-match negatives for generic head terms (e.g. [forklift charger] / [forklift battery charger] on charger modules, so module/PN long-tail like "hawker charger module" or "6la20671" still matches), phrase negatives for gas/carbureted-cart queries on Navitas (kits fit electric carts only; keep motor-upgrade/"make my cart faster" queries), and negatives for electric-motorcycle/generic EV-research/96V queries on lithium (72V golf-cart searches correctly match the 76V pack—valid fitment, keep them) plus waste negatives in `scripts/ads/2026-08-27-lithium-waste-negatives.py` (phrase ebike/e-bike/trolling motor/marine battery/scooter; exact [ev battery packs] and [lithium battery]); tracks waste negatives for near-me/tractor/used-machine/wrong-model/machine-for-sale (do not phrase-neg generic `for sale` so `t190 tracks for sale` still matches); commit negatives/mutation scripts under `scripts/ads/` alone without sweeping in unrelated working-tree changes. Customer-facing safety copy uses the "Try before you certify" framing — app is free, all 5 modules study free, $49 paid only at the final exam; keep same-$49 wording so users don't think they pay twice (web is the primary mobile CTA; app is secondary), and Spanish forklift wording must use "montacargas" (not "carretilla elevadora"). Before flipping paid ads on, run a strictly read-only PASS/FAIL/NEEDS-ATTENTION funnel verification with evidence (live HTTP 200 on the canonical URL and ad-targeted state params, hero/CTAs/FAQ/sticky CTA rendered in the target language with no English bleed, state personalization for each ad-targeted state with graceful fallback for unknown states, GA4 `app_download_click` firing with a locale-specific source while preserving `gclid`/UTM/`state` into the store URL, Stripe checkout endpoint reachable, legacy URL 301-redirects, `<html lang>` plus `hreflang` alternates linking EN↔ES, dual App Store + Google Play badges) and a clear blocker/non-blocker list before any code change; Spanish `/es/safety` uses the same web-checkout-first mobile CTA as EN—web post-checkout training (`/training`, exams, credential email) stays English-only until locale propagation ships.
- When customers request a certificate copy, return `certificates.pdf_url` plus `www.flatearthequipment.com/verify/{verify_code}` (resend via Resend if asked); when customer-paid certificates need to be reassigned to a different operator, update the enrollment/certificate name, regenerate the QR verification, send a new certificate email, and return a shareable PDF URL; the certificate PDF itself must brand as "Forklift Certified" (matching the app) rather than Flat Earth LLC for credibility, carry an employer-filled "Supervisor / Qualified Evaluator" signature line (not a Flat Earth signature), and never print unverifiable claims such as a practical-evaluation-verified date — the hands-on eval often happens after issuance outside the app/web flow, so evaluation data surfaces only on the `/verify` page when it exists.
- For formal customer quotes, deliver one-page PDFs on company letterhead saved under `/public/quotes/FEE-Q-YYYY-MMDD-NNN.pdf`, return a shareable download URL, and use soft/professional phrasing on payment terms (avoid blunt language such as "purchase orders are not accepted"); always commit new quote files under `public/quotes/` to git in the same session they are created (never leave them untracked) — the shareable URL is only live on production after the commit is pushed and deployed.
- Treat the brand canyon-rust orange as an accent color (eyebrow pills, single primary CTAs, hover/focus rings, small icons), not as full-section flood; for premium-feel sections sandwiched between white blocks (e.g. the homepage Training CTA) prefer a dark slate band with orange-on-dark accents, and favor editorial/asymmetric layouts with real product photography and visible whitespace over symmetric multi-card grids that read as filler.
- On `/battery-chargers`, lead with voltage cards → featured shoppable grid (`#chargers`) and quote CTAs—not wizard-first UX; remove broken `ModelFilterBanner` strips that claim fitment without filtering; keep the 3-step charger selector demoted as an optional helper and preserve `#charger-selector` for legacy deep links; retarget insights/charger CTAs to `#chargers` or `/quote`, not selector-first paths; `/chargers/{slug}` overlays `parts` photo/price when `parts_catalog` is empty so PDPs stay shoppable, but GREEN voltage×amp SKU pages stay noindex (canonical to the series page) so they do not flood Google—index `/battery-chargers`, the GREEN2/4/6/8/X series pages, and distinct branded SKUs (Delta-Q, Powerwise, reman) only.

## Learned Workspace Facts
- Workspace `flat-earth-equipment` is a Next.js App Router site deployed on Vercel for `www.flatearthequipment.com`, selling business-critical OSHA forklift certification, equipment parts, and rentals; legal entity is Flat Earth LLC (DBA Flat Earth Equipment); HQ is Sheridan, WY 82801 (Organization JSON-LD in `app/layout.tsx`); vendor/legal mailers use `Flat Earth LLC` / `(DBA Flat Earth Equipment)` / Sheridan HQ address; Denver Metro is parts-shipping only—no local rentals or field repairs; Supabase backs auth/data and production migrations, Stripe handles paid web checkout with `automatic_tax: { enabled: true }` on `/api/checkout` sessions (tax exclusive, calculated from billing/shipping address; Stripe Tax registered for Colorado collection only—Wyoming HQ has no state sales tax; TVH wholesale sales tax is a vendor resale-certificate problem, not a Stripe checkout change—do not register 24 SST states without a CSP, and Stripe Tax Complete is too expensive for this volume; tracks drop-ship tax is the same class of problem—the user is filing MTC Uniform + Texas resale certificates with the tracks vendor (not live yet), so do not assume wholesale tax is covered; FSIP (legal name Flight Systems Industrial Products Company, Carlisle PA) requires a resale certificate per ship-to state and takes Pennsylvania form REV-1220 at `taxexempt@fsip.biz`—Flat Earth holds no PA sales tax license, so leave the License ID blank and use the box-8 out-of-state-reseller/goods-for-resale explanation without dollar-threshold claims; PA accepts the MTC Uniform certificate for resale but not the SST form), Resend sends transactional email, and public mailboxes are `contact@flatearthequipment.com` and `parts@flatearthequipment.com`; Stripe `checkout.session.completed` also fires additive fire-and-forget Telegram private-chat sale alerts via `lib/telegram/notifySale.ts` (parts: PN/price/shipping city; training: plan/price; missing `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` = no-op; never blocks webhook success or fulfillment—never commit bot tokens; the session re-retrieve must expand only `line_items` and read shipping city/state from `collected_information.shipping_details`—expanding `shipping_details` 400s on the current Stripe API and silently drops every alert because the helper swallows errors; repeat alerts for the same checkout days apart are not new sales—they are Stripe retries caused by a failing endpoint on the shared Stripe account (`pending_webhooks: 1` on the event), so check endpoint delivery attempts; the China Lift Supply `chinaliftsupply.com` webhook was disabled for this and must only be re-enabled once it returns 200 (ignore, not error) for non-CLS sessions); `app/robots.ts` is the canonical robots source (`next-sitemap.config.js` uses `generateRobotsTxt: false`); the GitHub repo is `koolranch/flat-earth-equipment` (branch `main`), and scheduled monitoring runs as Vercel crons (renewals, reactivation AM/PM) plus read-only Cursor Automations (daily order-to-access reconciliation, daily checkout funnel smoke test, weekly Supabase advisor sweep, weekly quote follow-up digest — the digest reads committed `public/quotes/` files from the repo checkout) and weekly DataForSEO rank programs for charger modules, rubber tracks, lithium batteries, and cab glass under `docs/projects/{charger-modules,rubber-tracks,lithium-batteries,cab-glass}-revenue/` (README + STATUS) via `scripts/seo/charger-rank-check.ts`, `scripts/seo/rubber-track-rank-check.ts`, `scripts/seo/lithium-rhino-rank-check.ts`, `scripts/seo/navitas-rank-check.ts`, and `scripts/seo/cab-glass-rank-check.ts` writing dated snapshots to `scripts/seo/rank-snapshots/{charger,rubber-tracks,lithium-rhino,navitas,cab-glass}/` (Cloud Agent secrets `DATAFORSEO_LOGIN` / `DATAFORSEO_PASSWORD`; lithium keyword→winning URL map in `constants/lithiumRhinoSeo.ts`; cab-glass hub at `/cab-glass` with `category_slug=cab-glass`); the weekly rubber-tracks rank automation opens draft PRs rather than committing to `main`, so each week's snapshot/STATUS PR must be marked ready and merged or the data never lands (Aug 17 ran on an unmerged branch, Aug 24 never committed).
- The companion mobile app is Expo/React Native at `~/Documents/forklift-certified/forklift-certified/` with bundle/package `com.flateartheequipment.forkliftcertified`, URL scheme `forklift-certified`, Universal Links on `www.flatearthequipment.com`, App Store listing `https://apps.apple.com/us/app/forklift-certified-pro/id6759796469`, Google Play listing `https://play.google.com/store/apps/details?id=com.flateartheequipment.forkliftcertified`, and source code outside this Next.js repo; claimed web/app paths include `/invite/*`, `/claim/*`, `/enterprise/*`, `/trainer/*`, and `/app/*` but not `/login`; Forklift Certified marketing fronts `getforkliftcertified.com` and `certificacionmontacargas.com` have no Stripe checkout—purchase and fulfillment stay on `www.flatearthequipment.com` (`/safety`, `/es/safety`); iOS is live (`IOS_APP_LIVE=true`), so footer/`AppDownloadCTA` use `MobileAppBadges` for dual App Store + Play (not Play-only); any web-side store badges (e.g. on `/safety` and the sticky CTA) must render as `<button type="button">` rather than native `<a href>` so iOS Safari does not intercept before JS runs, and click tracking must flow through the shared helpers in `lib/analytics/app-download.ts` (fire `gtag('event','app_download_click', { transport_type: 'beacon' })`, briefly wait for `window.gtag` readiness, then `window.location.href` to the URL built by `buildStoreDownloadUrl` in `lib/app-store/links.ts`, which appends UTM params and forwards any `gclid` so Google Ads attribution carries through to app installs).
- The public website sends GA4 events to the "Flat Earth Equipment" web property via measurement ID `G-3MQWE0RK5M`, wired through `NEXT_PUBLIC_GA_MEASUREMENT_ID` (.env, `.env.production.local`, Vercel env, and a hardcoded fallback in `app/layout.tsx`); the mobile app reports independently to the Firebase-backed GA4 property `forklift-certified-app-e88c2`, so web events must always use the web measurement ID and never the app/Firebase property.
- Training includes single-operator and multi-seat/team purchases (Team 5-Pack $225, Team 25-Pack $999 per `lib/training/plans.ts`—Stripe prices are immutable, so create new price IDs and archive legacy IDs when changing), enterprise/org dashboards, certificates, enrollments, seat invites, and practical evaluations; `/enterprise/dashboard` and `/trainer/dashboard` are shared routes scoped by the logged-in user/org membership (future work may add per-org URLs); mobile manager APIs under `/api/mobile/manager/*`, learner GET/PATCH `/api/training/progress`, POST `/api/training/modules/{contentSlug}/steps/{stepKey}/complete`, and POST `/api/training/quiz-complete` all accept `Authorization: Bearer <Supabase access token>` via `lib/supabase/mobile-auth.ts` while keeping the existing web cookie path intact. Production `enrollments` has no UNIQUE on `(user_id, course_id)` (~12 duplicate pairs exist); `ensureForkliftEnrollment` in `lib/training/ensure-forklift-enrollment.ts`, gated by `ENSURE_ENROLLMENT_ON_PROGRESS=1`, is insert-only SELECT-then-INSERT (newest `created_at` row wins, never updates progress_pct/resume_state/cert fields, never called from Stripe webhooks, quiz-complete, video-complete, validate-code, claim-training, redeem-seat, or enterprise assign). The 5 forklift modules use registry content slugs `pre-operation-inspection`, `eight-point-inspection`, `stability-and-load-handling`, `safe-operation-and-hazards`, `shutdown-and-parking`, each with step keys `osha|practice|cards|quiz`; the shared `mergeStepIntoEnrollment` helper in `lib/training/merge-step-enrollment.ts` OR-merges `resume_state` step flags (never wipes prior keys) and stores `progress_pct` as a 0–100 integer with a `Math.max` safeguard so existing 100% purchasers never regress; `canTakeExam` is derived from `quiz_attempts` (≥5 passing module attempts) and "purchased" status is read from `/api/training/exam/status` + orders, not from GET progress 200 vs 404. Mobile `syncStepProgress` calls POST step-complete first and PATCH `/api/training/progress` as a fallback. Practical-eval invites use `/invite/practical-eval/[token]` and `forklift-certified:///invite/practical-eval/<token>` before generic `/invite/:code`. Certificate transactional emails fire only after the final exam is passed ("Exam passed — Certificate ready" from exam submit, then "Certificate issued" when the PDF is generated via `/api/cert/issue`); welcome/access emails are separate and earlier. To distinguish purchase channel in `orders`, web Stripe checkouts use `stripe_session_id` `cs_live_*` at $49.00; Android Google Play uses `GPA.*` at $49.99 with mobile module step slugs in `resume_state` (study-free-then-pay-at-exam funnel). The certificate PDF uses one shared EN/ES template serving both web and app users, branded "Forklift Certified" issued by Flat Earth Equipment, with the `seal_forklift_certified.png` seal on the Supabase CDN (legacy `seal_orange.png` must stay — other code still references it); `.cert-preview/` is an intentionally untracked local folder of certificate mockup renders and scripts.
- Per-brand serial-number lookup tools live at `/<brand>-serial-number-lookup`, each backed by Supabase data tables (`<brand>_model_cues`, `<brand>_plate_locations`, and where applicable `<brand>_module_dictionary`) and a `/api/<brand>-lookup` route (the older note that `/kubota-serial-number-lookup` is the top organic page is stale—it now earns only a handful of clicks a month; per GSC the current top organic pages are the Gehl and New Holland serial lookups under `/parts/construction-equipment-parts/`, `/takeuchi-serial-number-lookup`, and `/brand/crown/serial-lookup`); the premium template (server-rendered JSON-LD with Org+WebSite+WebApplication+HowTo+FAQPage schemas, FAQ, CTA, and a parts-fit card) is applied to Kubota, Bobcat, JCB, Case, and Takeuchi (CAT still pending an architectural decision); per-page JSON-LD must be emitted as a raw inline `<script type="application/ld+json" dangerouslySetInnerHTML>` tag (matching `app/layout.tsx`), not via `next/script` with `afterInteractive`, so search engines see it in the server-rendered HTML — `components/state/StateProductJsonLd.tsx` on `/safety/forklift/[state]` still uses `next/script` and should be migrated to match; legacy certification keyword paths (e.g. `/forklift-certification`) 301 to `/safety`, and the `/training` tree is noindexed crawl-only; `/safety` and `/safety/forklift/[state]` share `app/safety/components/SafetyScreenshots.tsx` (state pages use `compact` between social proof and penalties)—`[state]` codes are US state abbreviations (`de` = Delaware, not German), and indexing is gated by `shouldIndex` in `lib/safety/stateMetrics.ts` (Delaware currently noindexed); `/quote` accepts deep-link prefill via `?sku=`, `?equipment=`, and `?notes=`, and uses `alternates.canonical = '/quote'` so parameter variants don't fragment SEO; keyword rank tracking uses the DataForSEO API (credentials in env, account under `parts@flatearthequipment.com`) via `scripts/seo/rank-snapshot.ts` and category scripts (`rubber-track-rank-check.ts`, `cab-glass-rank-check.ts`, etc.), writing dated JSON snapshots to `scripts/seo/rank-snapshots/` including forklift-cert `forklift-cert-rankings-*.json` and `forklift-cert-hitlist-*.json` — prefer this API over scraping for SEO rank work; for real click/impression data use `scripts/seo/gsc-snapshot.ts` (28-day Search Console pull to `scripts/seo/rank-snapshots/gsc/YYYY-MM-DD.json`) rather than the Ahrefs MCP, whose plan returns "Insufficient plan" on site-explorer endpoints—the GSC OAuth refresh token at `~/.mcp-servers/gsc/token.json` expires about every 7 days (Google testing-mode app) and must be re-consented first via `~/.mcp-servers/mcp-google-ads/.venv/bin/python ~/.mcp-servers/gsc/reauth_gsc.py`; GSC shows site-wide organic clicks fell ~80% (≈4.3k/mo in May 2025 to ~500–900/mo) at the June–July 2025 core update, driven by legacy serial-lookup/fault-code pages under `/parts/`, `/rental/`, and `/diagnostic-codes/` losing rank at unchanged URLs (not by URL migrations), and 93 of the ~180 `content/insights/` posts are an Outrank.so-generated batch (cdn.outrank.so hero images, raw HTML bodies, published Nov 2025–Feb 2026) of which only the JCB/Case machine-fault posts earn clicks—treat the rest as thin-content candidates for rewrite or noindex.
- Repeat training purchases by an existing checkout email still create a new order/enrollment, but only the first checkout sends welcome credentials; subsequent purchases fire the additive, try/catch `/api/send-enrollment-notification` webhook call (single-seat → login notice, multi-seat → trainer-dashboard instructions with `isTrainer`) without modifying the new-user welcome path; multi-seat buyers also get `role: trainer` and one purchaser enrollment per order—the trainer dashboard roster shows claimed/active learners only, not pending `seat_invites`; additional multi-seat pack purchases by an existing org create Stripe orders via webhook but do not automatically increment `org_seats.total_seats`—the seat pool requires a manual post-payment update. Production currently lacks the `customer_orders` and `order_line_items` tables referenced by the parts webhook, so do not infer parts revenue from the training-centric `orders` table; use Stripe/GA4 evidence until parts attribution is repaired.
- Account deletion must preserve required business/payment records while deleting user-owned training/profile data where appropriate; the public privacy disclosure has multiple URLs that should stay synchronized: `/privacy`, `/privacy-policy`, `/legal/privacy`, and `/policies/privacy-policy`.
- Parts catalog uses Supabase + Stripe products/prices + product images and brand/category metadata; `part_number_slug` is the unique DB/URL identifier, `oem_part_number_display` is the customer-facing title, `parts.compatible_models` is an array used for fitment/brand interlinking/SEO, and `parts.is_fast_moving` is a boolean (default false, partial index on true) that lookup-tool parts-fit cards sort by `is_fast_moving DESC` so flagged SKUs surface first; aftermarket JCB, Bobcat, forklift parts, and rubber tracks carry a 2-year warranty via `lib/parts/aftermarketWarranty.ts` (chargers, controllers, charger modules, and core-charge SKUs are excluded and keep product-page coverage); parts sales scope excludes Kubota tractor and RTV parts even though the Kubota serial-lookup tool drives SEO traffic; the industrial seat guide is parsed to ~230 SKUs in `data/seats/` (`scripts/parse-seat-guide.ts`, `scripts/import-seat-stubs.ts`) with hub-priority rollout constants in `constants/seatModelHubs.ts`; internal Toyota OEM index `toyota-parts-catalog.csv` (strip `TY` prefix → OEM PN) rolls out as `quote_only` stubs like JCB in prioritized batches, not bulk publish; TVH top-PN and CPA cab-glass rollouts publish OEM brand + OEM number only (canonical OE-prefix map in `lib/parts/tvhOePrefixes.ts`) with the `/cab-glass` hub (model-selector SEO similar to rubber tracks; PDPs cross-link the hub; managed via `docs/projects/cab-glass-revenue/`). TVH-network inventory+sticker watch lives in `docs/projects/tvh-inventory-watch/` (scope in `lib/pricing/magWatchUniverse.ts`; weekday Firecrawl snapshots via `scripts/pricing/mag-watch.ts` (A/B/C/D plus ≤3-qty sell-out overlay, plus a capped private-tray hero intake) onto `parts.metadata.competitor_prices` + `mag_watch`; pull/relist/reprice/publish via `lib/pricing/magWatchOps.ts` from both `scripts/pricing/mag-watch-apply.ts` and the noindexed `/parts-watch` dashboard (Reprice: hold/verify gates; Publish: quote-only Mag-in-stock stubs, real-photo gate; image tray with AI clean on all SKUs, user-approve before `parts.image_url`), audited in `parts_ops_audit`); Magnasource URLs are `https://www.magnasourceinc.com/itemdetail/{PREFIX}{OEM}` with separators kept (JCB slash, Toyota dashes)—HTTP 200 is not proof of a valid item; hero facts may be recorded on `mag_watch.hero` (filename + identity gate) but never auto-set `parts.image_url` from the scrape. Rubber tracks are a product category: the vendor portal looks up tracks by brand/model and keys fitment to OEM serial-prefix breaks (e.g. Bobcat T650 breaks A3P0/A3P1/ALJG/B5FF/T1ML); Caterpillar 259D and 259D3 are separate vendor model filters—verify all serial breaks on both before combined listings even when core PNs appear shared; vendor PNs use a `TSA/` prefix where `TSA/SY<width>X<pitch>X<links>` encodes size (mm width × pitch × link count) and `TSA/BC<number>` embeds the Bobcat OEM cross-reference PN; tracks ship free from the vendor with a 2-year warranty (use the `free_freight` flag — no freight line, and free-shipping + 24-month warranty are the standard banner selling points versus comps that add $100–300 LTL); batch intake accumulates in `data/tracks/rubber-track-intake.csv` (model, vendor PN, OEM PN, size, pattern, serial prefixes verified, cost, sell price, comp evidence), keyword research runs via DataForSEO through `scripts/seo/rubber-track-keyword-research.ts`, and live URLs follow `{brand}-{model}-rubber-track-{size}` under `/parts/`; track UX (free-shipping badge, 2-year warranty, qty default 2, serial-prefix fitment block, `RubberTrackVisual` when no photo, landed pair pricing, sibling cross-links, fleet quote CTA) lives in `app/parts/[slug]/ProductDetails.tsx`; track product pages should cross-link two-way with the matching brand serial-number-lookup pages ("not sure? find your plate"); parts catalog brand sidebar filters load logos from the Supabase `brand-logos` bucket (e.g. `bobcat.webp`); `parts.image_url` must store relative paths or Supabase CDN URLs—absolute own-host URLs 400 through the next/image optimizer, so the affected track/glass/seat rows were normalized to relative paths (own host is now also in `next.config.js` `remotePatterns` as a safety net), and PDP Product JSON-LD `image` must be absolutized via SITE_URL since raw JSON-LD does not get `metadataBase` resolution; the 400-wide C-pattern T190/T550/T590 Shopping SKUs share a cleaned real product photo (`public/images/parts/tracks/bobcat-400x86x49-c-pattern-track.jpg` and matching per-slug copies); remaining track JPGs are still copies of the studio hero, while the ~103 cab-glass images are genuinely distinct per SKU; `public/images/parts/placeholder.jpg` is SVG content with a `.jpg` extension (400s through the optimizer), so no-photo catalog cards render an inline placeholder instead and the Merchant feed already excludes placeholder rows; `is_fast_moving` is set on rubber tracks only, so the `/parts` "Recommended" sort reads a ranked read-only view that interleaves categories (category rank before `is_fast_moving`) to keep page 1 diverse instead of all tracks, and sidebar brand/category facet counts must come from aggregate views—counting rows client-side from a plain select silently truncates at Supabase's default 1000-row cap once the catalog exceeds it (JCB showed 373 vs the real 738; Tennant/Skyjack/JLG dropped entirely); the sidebar itself uses a single scroll context (no nested scrollboxes—expanders inside fixed-height scroll containers render new content invisibly), top-8 brands with a show-all expander, a collapsible JCB group, clear-all at top, and toggle-off active filter rows; Google Merchant feed regenerates to `public/feed/google-merchant.{xml,json}` (live URL `https://www.flatearthequipment.com/feed/google-merchant.xml`) via `scripts/build-merchant-feed.ts` as a keep-set of rubber tracks, cab glass, lithium, Navitas kits, seats, reman charger modules, photo-ready JCB, and already-approved JCB brand-logo heroes (do not drop those logo SKUs—removal archives the approval); `isRemanOrRepairService` matches `\breman\b` in name/description and silently drops the SKU from `google-merchant.xml`, so do not use the word reman in JCB/aftermarket Buy Now copy unless the SKU actually is reman; Google’s scheduled fetch only reads the committed XML (it does not rebuild from Supabase), so description/price/freight edits need a script rebuild + commit + deploy before Shopping/XML updates; Buy Now SKUs outside those category rules that need Shopping go in `MERCHANT_KEEP_SLUGS` by slug only (do not expand whole brands/categories); never rewrite `g:id` on existing/approved items, never invent per-slug image paths, omit `placeholder.jpg` rows from the feed, advertise free shipping when `free_freight` applies, use `metadata.freight_cents` for per-SKU Ground when set so Shopping matches checkout, and advertise $25 Ground on cab glass (never $0); when `metadata.list_price_cents` exceeds the charged price, emit Merchant `g:price` as the list and `g:sale_price` as the charged amount so Shopping cards match the PDP strikethrough; Merchant Center currently approves via the “Found by Google” site crawl (mixed catalog; empty Content API), not the XML, until the scheduled fetch of `https://www.flatearthequipment.com/feed/google-merchant.xml` is Active—do not remove or hide Found by Google until then, and do not add a second colliding source; once the XML is Active, turn off ads on the crawl so quote-only/non-keep-set SKUs do not enter Shopping. Track `image_link`s keep stable per-slug JPG paths (C-pattern Shopping SKUs use the cleaned real product photo; others keep the studio hero—do not regenerate text-overlay cards onto live Merchant paths).
- Freight is added as a Stripe checkout line (not on product pages) and `freight_cents` is charged once per line by default (not × qty; wheels and other small-parcel SKUs stay once-per-line and must not double at qty 2; heavy SKUs opt into `freight_per_unit`); Navitas golf-cart controller conversion kits skip the freight line via `metadata.free_freight` (same checkout/PDP/Merchant pattern as rubber tracks—match Cloud Electric free-shipping comps); default/JCB parts freight is Ground $17 / $24 / $29 / $35 by sell-price band with a $35 cap (do not charge $49 — that reads as LTL; do not raise sell toward Magnasource just to drop a freight band); known TVH net ≥ $650 and not LTL uses `free_freight`, TVH-sourced parts (including CPA cab glass, which is small-parcel not LTL) use the TVH matrix ($18 / $25 / $31 / $37 / $41 across the same bands, with surface freight prepaid over $650 net for eligible stock SKUs—confirm or quote excluded/heavy items); cab glass stays on that matrix (typically $25 Ground under $150)—do not default glass to `free_freight`, forks use a flat-rate fork freight (~$395 to absorb actual carrier costs), seat assemblies use a $25 flat-rate checkout band (`Seats` category) and seat cushions/covers default to $29 unless vendor net is over ~$650 (TVH prepaid free freight) or a known true freight is set via `metadata.freight_cents` (e.g. with-switch vs no-switch Columbia twins)—Merchant must follow the same `freight_cents` when present, and JCB OEM parts are benchmarked against magnasourceinc.com and priced ~5% below comparable listings; lithium is sellable and freight is a margin cushion not a conversion blocker (actual FSIP outbound is regular ground, e.g. ~$85 PA→AZ on the 65Ah kit vs the old $149 charged—not a separate HazMat LTL bill); a Shopping free-freight test is live on three Demand-kit SKUs only (`113-LR51V65AH`, `113-LR38V105AH`, `113-LR51V105AH`)—checkout and Merchant must both show $0 Ground; leave 120Ah / 72V / Goliath / battery-only on paid HazMat bands; comp-based seat/TVH pricing lives in `lib/pricing/` (default 5% below lowest trusted Intella/Magnasource/comp, margin floors, provisional comp-only when wholesale cost is unknown)—Magnasource/Intella itemdetail pages are often bot-blocked to agents, so use GC Iron, Alpine/Shopify dealers, eBay, and manual SERP comps when needed.
- Charger module SKUs (Hawker, Enersys, ACT Quantum) are sold as `Repair and Return` or `Reman Exchange` variants on the `/charger-modules` hub with dedicated PN pages under `/charger-modules/{slug}` (`/parts/battery-charger-modules` redirects to the hub), with a core charge applied on reman exchange; repair & return marketing copy should not promise a prepaid return label unless inbound prepaid freight is actually included (fulfillment via FSIP repair receiving); charger Buy Now uses the simple `{priceId, slug, qty}` checkout path and does not yet add a freight line (unlike lithium weight-banded freight in `/api/checkout`)—heavy modules need a separate scoped freight-tier change after confirming FSIP outbound LTL costs; the category is managed as a revenue program in `docs/projects/charger-modules-revenue/`, whose STATUS.md records confirmed FSIP unit economics: 6LA20671 reman exchange costs $526 (covers both the Enersys and Hawker listings at $749 sell) and Repair & Return costs $444 with the live Stripe R&R price still $650/unit leftover from a prior custom 2-unit order (not a $1300 single); the three ACT Quantum 36V/48V/80V remans (Hyster/Yale cross-ref PNs 81063658R/81063577R/81063578R, $800 sell + $350 core) cost $513 each, with $30–50 absorbed outbound freight assumed until the first shipped order and a ~$30/order core-handling admin haircut applied to reman margins vs drop-ship categories. The same FSIP vendor supplies Navitas golf-cart controller conversion kits (customer brand Navitas only—do not name FSIP/Flight Systems in customer copy): hub `/navitas-controllers` should lead cart-first (match the label under the seat / OEM row chips → Recommended 600A/440A buy path) with kit jargon demoted to trust chips, TAC3 as a secondary text link, and lithium as a clear “Pair with…” CTA; kit data lives in `constants/navitasKits.ts`, Buy Now priced against Cloud Electric comps, and free freight via `metadata.free_freight` when matching comps.
- `org_members.role` includes manager-tier values (`owner`, `admin`, `manager`, `trainer`, `super_admin`) and learner-tier values (`learner`, `member`, `team_member`); web and mobile permission logic should stay aligned with this set—manager-tier roles grant enterprise dashboard access (web login lands on `/enterprise/dashboard`) but not automatic course enrollment; learners need an explicit `enrollments` row tied to the org seat pool. Enterprise/org/trainer API routes that accept a caller-supplied `org_id` must verify the authenticated user's membership (and appropriate role) via `supabaseService()` against `org_members` before returning data—match `enterprise/export/roster` and `enterprise/certificates/scores`; service-role clients bypass RLS, and the enrollments policy `enr_staff_r` (`is_staff` = global `profiles.role` admin/trainer) can expose cross-org reads if membership is not checked explicitly.
- Homepage (`app/page.tsx`) section order: parts-first Hero (credibility + Browse Parts CTA to `/parts`; secondary link to `/safety` for certification—not a forklift-cert primary hero; headline/sub cover forks, seats, lithium, chargers, and construction equipment parts broadly, not “industrial”/JCB-only) → dark `components/home/TrainingCTA.tsx` (slate-950 band with orange-on-dark accents, keep this certification band) → curated `components/FeaturedProducts.tsx` (hand-picked `FEATURED_SLUGS` array — currently lithium battery, Class IV forks, Toyota seat — fetched server-side from Supabase by slug rather than the unreliable `parts.featured` flag) → brands → testimonials → `components/FeaturedRentals.tsx` (editorial split layout, real photos preferred over `/rental-icons/*.svg` fallbacks; skip a price column and lead with specs like capacity/lift/power plus a "Get Quote" CTA) → geo coverage → features; preserve the hero trust row when editing hero copy; the homepage module set stays identical for all visitors (locale-based personalization via `getUserLocale` only — no IP-based service-area gating); centralized safety/training marketing copy lives in `i18n/marketing.en.ts` and `i18n/marketing.es.ts` keyed by the shared `MarketingDict` type and read by SafetyHero, AppDownloadCTA, PricingStrip, and the sticky CTA.
