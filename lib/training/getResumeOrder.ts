// Stub file for resume order calculation
export async function getResumeOrder(userId: string, courseId: string): Promise<number> {
  // In a real implementation, this would check user's progress
  return 1; // Default to first module
}
