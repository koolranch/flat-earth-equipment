'use client';

/**
 * @deprecated Prefer `MobileAppBadges` — kept as a thin alias so older imports
 * still get both App Store + Google Play now that iOS is live.
 */
export { default } from './MobileAppBadges';
